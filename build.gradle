apply plugin: 'com.android.library'

android {
    namespace 'com.relaxpic.android.i18n'
    compileSdkVersion compileSdkVersionNumber as int
    defaultConfig {
        minSdk minSdkVersionNumber
        targetSdk targetSdkVersionNumber
    }
}